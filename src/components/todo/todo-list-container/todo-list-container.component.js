import templateUrl from './todo-list-container.ngtpl.html';
import controller from './todo-list-container.controller';

const todoListContainerComponent = {
  templateUrl: templateUrl,
  controller: controller,
};

export default todoListContainerComponent;
